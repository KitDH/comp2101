#exploredatetime.ps1~~
#how to explore objects??

"This will display some info about the DateTime object from Get-Date"
"  The first step is to use (Get-Date).GetType().FullName
"

(Get-Date).GetType().FullName


